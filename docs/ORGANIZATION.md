# :file_folder: Organization

## :book: Table of Contents

- [:file_folder: Organization](./#file_folder-organization)
  - [:telescope: Overview](./#telescope-overview)
  - [:building_construction: Structure](./#building_construction-structure)
    - [:building_construction: Knowledge & Research](./#building_construction-knowledge-research)
    - [:building_construction: Implementation](./#building_construction-implementation)
    - [:building_construction: Templates & Tools](./#building_construction-templates-tools)
    - [:building_construction: Template Inheritance](./#building_construction-template-inheritance)
  - [:toolbox: Tools](./#toolbox-tools)
    - [:toolbox: `cmr`](./#toolbox-cmr)
  - [:books: References](./#books-references)

## :telescope: Overview

<!-- llm<Organization.Overview;applied> -->

chimera-lab.org is the organization-level super-repository for chimera-lab, acting as the central index for shared governance, documentation, and topic-based collections managed as Git submodules. Topic directories such as `arts.topic`, `humanities.topic`, `natural-sciences.topic`, and `projects.topic` group repositories by domain, while the organization’s semantic suffix conventions keep repositories categorized consistently by purpose.

This repository also serves as the common documentation and structure baseline for the organization. Root policy files define governance and contribution expectations, `docs/` holds deeper guidance on organization and structure, and the CMR CLI supports validation, template synchronization, and ongoing repository management workflows.● Writer Draft org overview
  │ Read README.md
  │   │ contributions.topic/README.md
  │   └ Path does not exist
  └ Todo completed organization-overview-body

chimera-lab.org is the organization-level super-repository for chimera-lab, serving as the central index for shared governance, documentation, and topic-based collections managed as Git submodules. Topic directories such as `arts.topic`, `humanities.topic`, `natural-sciences.topic`, and `projects.topic` group repositories by domain, while the organization’s semantic suffix conventions keep repositories categorized consistently by purpose.

This repository also provides the common structural and documentation baseline for the organization. Root policy files define governance and contribution expectations, `docs/` contains deeper guidance on organization and structure, and the CMR CLI supports validation, template synchronization, and ongoing repository management workflows.

chimera-lab.org is the organization-level super-repository for chimera-lab, serving as the central index for shared documentation, governance, and topic-based collections managed as Git submodules. It organizes work through semantic repository suffixes such as `.topic`, `.project`, `.app`, `.package`, `.scaffold`, and `.template`, so repositories are grouped consistently by purpose and remain easier to navigate and maintain.

Organization-wide standards for repository structure, documentation, and template inheritance are coordinated here, with detailed guidance in `docs/` and related topic directories such as `projects.topic`, `template.topic`, and the subject-area `.topic` collections. Governance is maintainer-led, while the CMR CLI supports validation, template synchronization, and other repository management workflows across the organization.● Technical-writer Draft org overview
  │ Read README.md
  │   │ personal.topic/README.md
  │   └ 30 lines read
  └ Todo completed draft-organization-overview

chimera-lab.org is the organization-level super-repository for chimera-lab, serving as the central index for shared documentation, governance, and topic-based collections managed as Git submodules. It organizes work through semantic repository suffixes such as `.topic`, `.project`, `.app`, `.package`, `.scaffold`, and `.template`, so repositories are grouped consistently by purpose and remain easier to navigate and maintain.

Organization-wide standards for repository structure, documentation, and template inheritance are coordinated here, with detailed guidance in `docs/` and related topic directories such as `projects.topic`, `template.topic`, and the subject-area `.topic` collections. Governance is maintainer-led, while the CMR CLI supports validation, template synchronization, and other repository management workflows across the organization.

● skill(write)

I’m checking the repository’s existing organization docs and taxonomy so the section matches the project’s language and structure.

● Read README.md
  └ L1:220 (124 lines read)

● Read AGENTS.md
  └ L1:220 (150 lines read)

● List directory docs
  └ 6 files found

● Search (grep)
  │ "Organization\.Overview|organization overview|## .*Overview|# .*Overview" in
  │ **/*.md
  └ 10 lines found

I found the repo’s organization doc and template content; now I’m pulling the exact organizational framing so the section reads like the rest of the docs.

● Read ORGANIZATION.md
  │ docs/ORGANIZATION.md
  └ L1:220 (212 lines read)

● Read STRUCTURE.md
  │ docs/STRUCTURE.md
  └ L1:220 (219 lines read)

● Read ORGANIZATION.md
  │ template.topic/overview.template/docs/ORGANIZATION.md
  └ L1:220 (111 lines read)

● Search (grep)
  │ "Organization\.Overview|llm<Organization|organization\.md|Managing
  │ entity|chimera-lab\.org" in **/*.md
  └ 9 lines found

chimera-lab.org is the organization-level super-repository for chimera-lab, serving as the top-level index for repositories, shared documentation, and topic-based collections managed as Git submodules. Its layout follows the organization’s semantic suffix conventions—such as `.topic`, `.overview`, `.project`, `.app`, `.package`, `.scaffold`, and `.template`—so repositories are grouped consistently by purpose and managed through a common structure.

Organization-wide standards for repository structure, documentation, and template inheritance are coordinated centrally, with updates propagated through the template hierarchy and supported by the CMR CLI. Governance is maintainer-led, with major structural, naming, and template decisions discussed through GitHub Issues or Discussions to keep the organization consistent and manageable.

● skill(write)

● Search (glob)
  │ "**/{README.md,docs/**/*.md,.github/agents/*.md,.github/prompts/*.md,**/*.to
  │ pic/**/*.md}"
  └ 10 files found

● Search (grep)
  │ "Organization\.Overview|## .*Overview|:telescope: Overview|organization" in
  │ *.{md,mdx,txt}
  └ 9 lines found

I’m pulling the repo’s existing organization language and header patterns so the section matches the documentation style.

● Read README.md
  └ L1:120 (119 lines read)

● Read README.md
  │ personal.topic/README.md
  └ L1:80 (30 lines read)

● Read writer.agent.md
  │ .github/agents/writer.agent.md
  └ L1:160 (60 lines read)

● Read write.prompt.md
  │ .github/prompts/write.prompt.md
  └ L1:160 (32 lines read)

● Search (grep)
  │ "part of the chimera-lab organization|chimera-lab
  │ organization|organization\.|organization" in README.md
  └ 29 lines found

● Search (grep)
  │ "Organization\.Overview" in *.{md,yml,yaml,json}
  └ 87 lines found

I found the exact placeholder and generation hint for `Organization.Overview`; now I’m reading the repo’s organization docs and adjacent templates to mirror the intended scope.

● Read ORGANIZATION.md
  │ docs/ORGANIZATION.md
  └ L1:120 (111 lines read)

● Read STRUCTURE.md
  │ docs/STRUCTURE.md
  └ L180:230 (1 line read)

● Read README.md
  │ projects.topic/chimera-lab-organization.project/README.md
  └ L1:120 (20 lines read)

● Read README.md
  │ projects.topic/README.md
  └ L1:80 (30 lines read)

● Read README.md
  │ original.topic/chimera-lab-cli.app/README.md
  └ L1:120 (110 lines read)

● Read settings.json
  │ .chimera-lab/settings.json
  └ L330:360 (26 lines read)

● Read README.md
  └ L1:120 (119 lines read)

● Read STRUCTURE.md
  │ docs/STRUCTURE.md
  └ L1:120 (120 lines read)

● Read AGENTS.md
  └ L1:160 (150 lines read)

● Search (grep)
  │ "semantic suffix|suffixes|topic
  │ repositories|super-repository|template-driven|multi-repository" in *.md
  └ 7 lines found

● Read GOVERNANCE.md
  └ L1:120 (53 lines read)

● Search (grep)
  │ "submodule|semantic suffixes|organization
  │ tree|template|ownership|governance" in *.md
  └ 8 lines found

I have the repo’s own cues now: this is the super-repository, organized by semantic suffixes, submodules, and template inheritance. I’m handing that context to the writer subagent to draft a concise section that fits the existing docs.

chimera-lab.org is the organization-level super-repository for chimera-lab, serving as the top-level index for repositories, shared documentation, and topic-based collections managed as Git submodules. Its layout follows the organization’s semantic suffix conventions—such as `.topic`, `.overview`, `.project`, `.app`, `.package`, `.scaffold`, and `.template`—so repositories are grouped consistently by purpose and can be managed through a common structure.

Management is centralized under a single maintainer, who holds final authority while major structural, naming, and template changes are discussed through GitHub Issues or Discussions. Organization-wide standards for repository structure, documentation, and template inheritance are coordinated centrally, with updates propagated through the template hierarchy and supported operationally by the CMR CLI.● Writer Draft overview section
  │ Read README.md
  │   │ original.topic/chimera-lab-cli.app/README.md
  │   └ 110 lines read
  └ Todo completed draft-organization-overview

chimera-lab.org is the organization-level super-repository for chimera-lab, serving as the top-level index for repositories, shared documentation, and topic-based collections managed as Git submodules. Its layout follows the organization’s semantic suffix conventions—such as `.topic`, `.overview`, `.project`, `.app`, `.package`, `.scaffold`, and `.template`—so repositories are grouped consistently by purpose and can be managed through a common structure.

Management is centralized under a single maintainer, who holds final authority while major structural, naming, and template changes are discussed through GitHub Issues or Discussions. Organization-wide standards for repository structure, documentation, and template inheritance are coordinated centrally, with updates propagated through the template hierarchy and supported operationally by the CMR CLI.

## :building_construction: Structure

```text
repository-name.suffix/
├── .chimera-lab/           # Repository metadata
│   ├── meta.json           # Description, tags, template
│   └── settings.json       # Configuration
├── .github/                # GitHub workflows
│   ├── agents/             # AI agent definitions
│   └── prompts/            # Prompt templates
├── docs/                   # Documentation and knowledge
│   └── knowledge/          # Knowledge base
├── README.md               # Main documentation
├── CHANGELOG.md            # Version history
├── CONTRIBUTING.md         # Contribution guidelines
├── CODE_OF_CONDUCT.md      # Community guidelines
├── SECURITY.md             # Security policy
└── LICENSE                 # License terms
```

Repositories are categorized by suffix:

### :building_construction: Knowledge & Research

- **`.topic`** - Knowledge areas, research, documentation collections
- **`.overview`** - High-level summaries and guides

### :building_construction: Implementation

- **`.project`** - Concrete implementations with deliverables
- **`.app`** - Standalone applications (web, mobile, desktop, CLI)
- **`.package`** - Reusable libraries/packages

### :building_construction: Templates & Tools

- **`.scaffold`** - Project templates and generators
- **`.template`** - Reusable document/code templates

### :building_construction: Template Inheritance

Templates are GitHub repositories that provide reusable structures. Repositories inherit from templates through a hierarchical chain.

**Template Hierarchy**:

```text
repository.template (base)
├── topic.template
├── overview.template
├── diy.template
├── app.template
│   └── laravel_app.template
├── project.template
├── scaffold.template
│   └── docker_scaffold.template
└── org.template
```

**How Templates Work**:

1. **Template Selection**: Repositories specify their template in `.chimera-lab/meta.json`
2. **Template Storage**: Template added as Git submodule at `.github/.template`
3. **Inheritance Chain**: Templates inherit from parent templates
   - Example: `laravel_app.template` → `app.template` → `repository.template`
4. **Updates**: Changes propagate down the hierarchy
   - Modify at the highest level that needs the change
   - Use `cmr repo template update` to sync with latest version

**Example Configuration** (`.chimera-lab/meta.json`):

```json
{
  "repo": {
    "template": "app.template"
  },
  "remote_template": {
    "repo": "chimera-lab.org/app.template",
    "branch": "main"
  }
}
```

## :toolbox: Tools

### :toolbox: `cmr`

The CMR CLI (`cmr`) automates chimera-lab.org repositories managing documentation validation, milestones, issues, labels, and git operations for submodules and templates. It unifies discovery, GitHub resources, and template workflows.

## :books: References

- [:page_facing_up: STRUCTURE.md](STRUCTURE.md)
- [:page_facing_up: ../README.md](../README.md)
- [:page_facing_up: knowledge/cmr.knowledge.md](knowledge/cmr.knowledge.md)
