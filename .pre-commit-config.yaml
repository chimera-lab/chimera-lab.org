repos:
  - repo: local
    hooks:
      - id: cmr-docs-check
        name: CMR docs check
        entry: cmr docs check --strict
        language: system
        types: [markdown]
        pass_filenames: false
        stages: [pre-push]
